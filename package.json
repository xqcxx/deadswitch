{
  "name": "deadswitch-tests",
  "version": "1.0.0",
  "description": "Run unit tests on this project.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:report": "vitest run -- --coverage --costs",
    "test:watch": "chokidar \"tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm run test:report\"",
    "test:coverage": "vitest run --coverage",
    "lint": "echo 'Run linting in frontend directory' && cd frontend && npm run lint",
    "format": "echo 'Run formatting in frontend directory' && cd frontend && npm run format",
    "build": "echo 'Build frontend' && cd frontend && npm run build",
    "ci": "npm run test && npm run lint && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@stacks/clarinet-sdk": "^3.9.0",
    "@stacks/transactions": "^7.2.0",
    "@types/node": "^25.2.2",
    "chokidar-cli": "^3.0.0",
    "vitest": "^4.0.7",
    "vitest-environment-clarinet": "^3.0.0"
  }
}
